<% if has_role?("Student") || has_role?("Instructor") %>
    <h1>You don't have the authorization to access this page...</h1>
<% end %>
<% if has_role?("Admin") %>
<h1>Edit Cohort</h1>
 
<%= form_with(model: @cohort, local: true) do |form| %>
  <div class="form-group">
      <%= form.label :name %><br>
      <%= form.text_field :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.label :course %><br>
    <%= collection_select(:cohort, :course_id, Course.all, :id, :name, {}, {:class=>'form-control'}) %>
  </div>
  <div class="form-group">
    <%= form.label :start_date %><br>
    <%= form.date_field :start_date, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.label :end_date %><br>
    <%= form.date_field :end_date, class: "form-control" %>
  </div>
    <%= form.button :Submit, class: 'btn btn-primary submit-button' %>
  </p>
 
<% end %>
<% end %>
